export const NUMBER_RANGE = Object.freeze({
  min: 1,
  max: 45,
});

export const Lotto_FORM = Object.freeze({
  range: NUMBER_RANGE,
  length: 6,
  price: 1000,
});

export const BONUS_BALL_FORM = Object.freeze({
  range: NUMBER_RANGE,
  length: 1,
});
